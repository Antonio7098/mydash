# Text Input

Apply `.mydash-text-input` to a label containing a visible label and native
single-line input.

```html
<label class="mydash-text-input">
  <span>Project name</span>
  <input type="text">
</label>
```
