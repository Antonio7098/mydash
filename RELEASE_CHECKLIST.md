# MyDash v0.1 release checklist

- [ ] `npm install` succeeds on a clean machine
- [ ] `npm run check:source` passes
- [ ] `npm run validate` passes
- [ ] `npm test` passes
- [ ] `npm run smoke` passes
- [ ] Navigator opens at `http://127.0.0.1:4173/`
- [ ] Reference dashboard renders and exports
- [ ] Standalone export works through `file://`
- [ ] Visual library browser shows Core and Local resources
- [ ] Preview-only appearance does not modify files
- [ ] Personal appearance survives browser reload
- [ ] Artefact-default appearance creates a focused commit when Git is available
- [ ] Navigator still opens from an extracted zip without Git
